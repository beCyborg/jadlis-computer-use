# jadlis-computer-use

Мысль: до кликов по экрану проверяются три более дешёвых пути, а кадр экрана — самая дорогая операция контура, и он снимается не на каждый шаг.

## hero

- Картинка: `docs/img/hero-jadlis-computer-use.webp`
- Заголовок: «Клики по экрану — последняя ступень, а не первый ход»
- Слева: лестница из четырёх ступеней с подписями «Свой интерфейс», «Системные команды», «Браузер», «Клики по экрану»
- Справа: нативное окно приложения на линии рабочего стола; подпись «Окно без своего интерфейса»
- Полоса внизу: «кадр экрана — самая дорогая операция»
- Названия брендов и сервисов в картинку не идут: только категории

```
Style: clean flat infographic on a light off-white background (#fdfbf7), two accent colors — deep teal (#1a7174) and warm orange (#f37d2c), thin dark-gray (#2f3333) line art, generous whitespace, geometric shapes, a modern geometric sans-serif look. Short Russian labels rendered as crisp, correctly spelled Cyrillic text. No robots, no glowing AI sparkles or glitter, no stock-photo people, no watermark. Wide 2:1 composition, 1280x640.

Hero illustration for a GitHub README about driving a native desktop application window only when nothing cheaper can reach it. Bold Russian headline across the top «Клики по экрану — последняя ступень, а не первый ход». On the left a flat outlined staircase of exactly four steps rising left to right, drawn as thin line art with no 3D depth and no solid fill, a small numeral 1, 2, 3, 4 on each tread, the three lower steps teal outlines and the topmost step an orange outline; a short Russian label sits beside each tread in small type: «Свой интерфейс», «Системные команды», «Браузер», «Клики по экрану». It must read as an order of methods tried from the cheapest, never as a bar chart. One thick orange arrow runs from the topmost step to the right side, where a large teal native application window frame has a title bar with three small round window controls and four short gray content lines, standing on a simple horizontal desktop line; a Russian label under it reads «Окно без своего интерфейса». Along the bottom one thin orange band with the Russian caption «кадр экрана — самая дорогая операция». No logos, no brand names, no service names. Text must be spelled exactly.
```

## scheme

- Картинка: `docs/img/how-jadlis-computer-use.webp`
- Блоки: «Задача на рабочем столе» → «Выбор пути» → «Один запрос доступа» → «Батч действий» → «Проверка по живому состоянию»
- Примечание под вторым блоком: «клики по экрану — последними»
- Примечание под четвёртым блоком: «в конце один кадр, а не после каждого шага»
- Названия брендов и сервисов в картинку не идут: только категории

```
Style: clean flat infographic on a light off-white background (#fdfbf7), two accent colors — deep teal (#1a7174) and warm orange (#f37d2c), thin dark-gray (#2f3333) line art, generous whitespace, geometric shapes, a modern geometric sans-serif look. Short Russian labels rendered as crisp, correctly spelled Cyrillic text. No robots, no glowing AI sparkles or glitter, no stock-photo people, no watermark. Wide 2:1 composition, 1280x640.

Cause-and-effect diagram with five rounded boxes in a row connected left to right by arrows, alternating teal and orange label plates, one simple geometric icon on top of each box: an application window frame standing on a horizontal desktop line; a fork in a path splitting into three short branches; a single key outline over a list of three application window shapes; three small action icons — a pointer, a keyboard key and a screen frame — grouped inside one bracket; a screen frame with a magnifying glass over it and one small square check field. Russian labels under the icons: «Задача на рабочем столе» → «Выбор пути» → «Один запрос доступа» → «Батч действий» → «Проверка по живому состоянию». Under the second box a small Russian caption in one line «клики по экрану — последними». Under the fourth box a small Russian caption in one line «в конце один кадр, а не после каждого шага». No logos, no brand names, no service names. Text must be spelled exactly.
```

## alt

- hero (`docs/img/hero-jadlis-computer-use.webp`): Три более дешёвых пути до задачи, и только за ними — клики по экрану
  Текстом: слева лестница способов — свой интерфейс приложения, системные команды, браузер и только наверху клики по экрану, — справа нативное окно, до которого ничем другим не дотянуться.
- scheme (`docs/img/how-jadlis-computer-use.webp`): Задача на рабочем столе, выбор пути, один запрос доступа, батч действий, проверка по живому состоянию
  Текстом: задача на рабочем столе → выбор пути → один запрос доступа → батч действий → проверка по живому состоянию.
